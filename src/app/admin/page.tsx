// import db from "@/db/db";

// async function fetchQuestionData() {
//   // find total number of questions, and number of questions created each day for the past month
// }

// async function fetchSystemData() {
//   // fetch number of questions associated with a unique system, category, and subcategory
// }

// async function fetchAdminData() {
//   // fetch top 5 admins with most questions created
// }

export default async function AdminPage() {
  // const [questionData, systemData, adminData] = await Promise.all([
  //   fetchQuestionData(),
  //   fetchSystemData(),
  //   fetchAdminData()
  // ])

  return (
    <main className="h-page">
      <p>Admin</p>
    </main>
  );
}
